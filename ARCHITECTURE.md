# 学术支持管理平台｜底层代码与业务逻辑

## 1. 建设结论

这个平台不是再造一套 BMP，而是做“学术业务中台”：

- BMP/CRM 继续作为项目、客户、审批和商机的主数据源。
- Synology 云盘继续作为招标证据原文件库。
- 学术支持平台负责规则、门禁、看板、预警、证据索引、复盘和跨模块经营分析。

```
员工 / 管理层
      │ SSO + RBAC
      ▼
学术支持管理平台
  ├─ 六大业务工作台
  ├─ 规则/门禁/SLA/预警引擎
  ├─ D1 统一索引、映射和审计数据
  └─ 接口适配层
       ├─ Synology WebAPI ── 招标资料原文件
       └─ BMP REST API ─── 项目/工单/会议/评审/培训
```

## 2. 已验证的云盘对接方式

- 入口：`https://sznas.ali.cnvseq.com/`
- 系统：Synology DSM / File Station
- 已通过实际 API 能力查询确认：
  - `SYNO.API.Auth` 版本 1–7
  - `SYNO.FileStation.List` 版本 1–2
  - `SYNO.FileStation.Search` 版本 1–2
- 登录及搜索在服务端完成，前端永远接触不到云盘账号和密码。
- 服务端每次搜索按“登录→搜索→停止任务→退出”处理，会话 SID 不落库。

核心代码：

- `lib/integrations/synology.ts`：登录、列表、搜索、超时、安全错误、会话清理。
- `app/api/tender/search/route.ts`：对内统一招标证据检索接口。
- `components/tender-search.tsx`：招标证据搜索界面。

## 3. BMP 对接原则

BMP 的实际网关、鉴权方式和字段文档尚未提供，所以平台采用“适配器 + 字段映射”，不将 BMP 字段直接散落在业务代码中。

页面、规则和看板只使用平台统一模型；BMP 字段变化时，只改适配层。

| 业务模块 | 建议 BMP 资源 | 关键同步字段 |
|---|---|---|
| 科研项目 | research-projects | 项目号、主项目号、客户、业务性质、阶段、里程碑、责任人、报价/合同/伦理/合规门禁 |
| 售后 | aftersales-tickets | 工单号、严重度、分类、SLA、责任部门、根因、CAPA、客户确认 |
| 会议 | academic-events | 会议目标、区域、客户、决策人、预算、线索、商机、销量转化 |
| PGD评审 | pgd-reviews | 中心、阶段、资料完整度、缺口级别、责任人、节点、整改证据 |
| 遗传咨询培训 | genetic-counseling-training | 中心、学员、路径、课程进度、个案实训、考试、认证 |

同步规则：

1. 首次全量拉取，之后用 `updated_after` 或 `cursor` 增量同步。
2. 用 `module + external_id` 幂等 upsert，避免重复写入。
3. BMP 是原业务记录的主数据；平台只保存统一索引、规则结果、预警、复盘和审计。
4. 任何同步失败记入 `sync_runs`，不得静默丢数。
5. 写回 BMP 时必须有版本号/ETag 或幂等键，防止重复审批和重复派单。

## 4. 六大模块的必须逻辑

### 招标中心

` 需求登记 → 参数拆解 → 证据定位 → 应答 → 复核 → 结果复盘 `

必须建立“技术参数—证据文件—页码/段落—资质有效期—复核人”关系。只返回文件列表不算完成招标智能化。

### 科研项目

` 需求登记 → 项目评估 → 正式立项 → 启动门禁 → 项目执行 → 验收/转化 `

一级分类必须是客情型科研、战略研发型科研、收费检测/过渡生产型；横向课题、多中心、数据分析等只是二级属性或交付类型。

### 售后闭环

` 统一受理 → 严重度分级 → 责任部门 → SLA → 客户确认 → 根因/CAPA `

学术支持部是对内对外统一接口，但不代替研发、医学、生产、质量承担原责任。

### 推广会议

` 会前目标 → 执行 → 7日行动 → 30日商机 → 90日销量 → 复盘 `

数量只是工作量；决策人覆盖、观念改变、商机和销量才是价值。

### PGD 资质评审

` 准入评估 → 差距诊断 → 整改计划 → 资料/SOP → 模拟评审 → 问题闭环 `

管制对象不是“文件数”，而是要求、证据、版本、责任人、完成时间和验收结论。

### 遗传咨询培训

` 能力测评 → 学习路径 → 课程 → 个案实训 → 考试认证 → 应用复评 `

课程完成率不等于能力达成率；必须增加个案胜任度和临床应用评估。

## 5. 账号、权限和安全

- 员工访问：平台 SSO，服务端识别用户。
- 业务授权：用户角色 + 大区/部门 + 模块权限。
- 云盘访问：Synology 服务账号，密码只在托管环境变量中。
- BMP 访问：建议由 IT 提供独立服务 Token，不复用个人账号。
- 所有关键查询、导出、审批、写回和权限拒绝记入 `audit_logs`。
- 不在源码、数据库、日志、前端返回或错误信息中出现密码、Token 或 Synology SID。

## 6. 建议实施节奏

1. **P0｜接口打通：** IT 提供 BMP 测试网关、鉴权、Swagger/Postman、数据字典和测试账号；确认 Synology 服务账号最小读权限。
2. **P0｜科研+售后先行：** 先解决现有 512 条台账分类、主子项目和售后 SLA，价值最直接。
3. **P1｜招标证据库：** 先做文件索引和资质有效期，再做 AI 参数拆解和证据定位。
4. **P1｜会议/评审/培训：** 统一过程数据和结果数据，形成季度复盘。
5. **P2｜智能分析：** 在数据口径稳定后再上 AI，避免把历史混乱放大。

## 7. 当前已完成的代码

- 管理总览及 6 个模块详情页。
- 招标云盘检索界面与 Synology 服务端连接器。
- BMP 统一连接器与增量同步接口框架。
- 用户识别、管理员权限和错误隔离。
- 项目、售后、会议、PGD 评审、培训、招标证据、同步和审计数据表。

## 8. 下一步必须获取的外部材料

- BMP 接口文档或 Swagger/Postman 文件。
- BMP 五个模块各 20–50 条脱敏样例数据。
- BMP 后台实际审批节点、条件分支和状态字典导出。
- Synology 服务账号可读的根目录及权限边界。
- 员工、部门、大区和角色权限矩阵。
